# Guide screenshots

Drop screenshots referenced by `src/pages/HelpPage.tsx` here, named
`schloss-<slug>.png` (see the `<img>` tags in that file for the exact
filenames expected). No code change is needed once a matching file exists —
Vite serves this whole directory as-is from the site root.
